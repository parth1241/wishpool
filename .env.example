# MongoDB Connection
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/wishpool

# Stellar Configuration
NEXT_PUBLIC_STELLAR_NETWORK=testnet
NEXT_PUBLIC_ESCROW_ADDRESS=GC...

# Secret Key (Keep this secure and NEVER commit it)
# Required for automated payouts
STELLAR_ESCROW_SECRET=S...
